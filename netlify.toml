[build]
command = "npm run build"

[build.environment]
NODE_VERSION = "22"

# Old ARFID redirects — removed. The ARFID content is retired.
# No redirects currently needed. Add if any old URLs need forwarding.